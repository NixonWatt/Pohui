@using MyRes = Resources.Resource
@model Pohui.Models.LoginModel

@{
    ViewBag.Title = "Выполнить вход";
}

<hgroup class="title">
    <h1>@ViewBag.Title.</h1>
</hgroup>

<section id="loginForm">
<h2>@MyRes.UseForLoginIntoLocalProfile</h2>
@using (Html.BeginForm(new { ReturnUrl = ViewBag.ReturnUrl })) {
    @Html.AntiForgeryToken()
    @Html.ValidationSummary(true)

    <fieldset>
        <legend>@MyRes.LoginForm</legend>
        <ol>
            <li>
              @MyRes.Name
                @Html.TextBoxFor(m => m.Login)
                @Html.ValidationMessageFor(m => m.Login)
            </li>
            <li>
              @MyRes.Password
                @Html.PasswordFor(m => m.Password)
                @Html.ValidationMessageFor(m => m.Password)
            </li>
            <li>
                @Html.CheckBoxFor(m => m.RememberMe)
               @MyRes.RememberMe
            </li>
        </ol>
        <input type="submit" value="@MyRes.LogIn" />
    </fieldset>
    <p>
        @Html.ActionLink(@MyRes.Register, "Register"), @MyRes.IfYouDontHaveProfile.
    </p>
}
</section>



